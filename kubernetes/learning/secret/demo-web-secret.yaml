# !!! LEARNING-ONLY, OBVIOUSLY FAKE VALUES. NEVER PUT REAL CREDENTIALS IN THIS REPOSITORY. !!!
#
# base64 is an encoding, NOT encryption: anyone who can read this file (or the Secret in the
# cluster) can decode it with `base64 -d`. Real secrets must come from a secrets-management
# mechanism (Phase: Secrets management), never from Git. See docs/kubernetes/fundamentals.md.
#
#   username = "demo-user"
#   password = "not-a-real-password"
apiVersion: v1
kind: Secret
metadata:
  name: demo-web-secret
  namespace: k8s-learning
  labels:
    app.kubernetes.io/name: demo-web
    app.kubernetes.io/part-of: k8s-learning
    learning-only: "true"
type: Opaque
data:
  username: ZGVtby11c2Vy
  password: bm90LWEtcmVhbC1wYXNzd29yZA==
